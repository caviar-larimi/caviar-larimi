'use client'; import Link from 'next/link';
import Image from 'next/image';
import {useLocale, useTranslations} from 'next-intl';
import {localeNames, locales} from '@/i18n/routing';
import {usePathname} from 'next/navigation'; export default function Navbar() { const t = useTranslations('nav'); const locale = useLocale(); const pathname = usePathname(); const pathnameWithoutLocale = pathname.replace(/^\/(de|en|fr|it)(?=\/|$)/, '') || '/'; return ( <header className="sticky top-0 z-50 border-b border-slate-200 bg-white/85 backdrop-blur"> <div className="container-page flex items-center justify-between py-4"> <Link href="/" className="flex items-center gap-3"> <div className="relative h-10 w-10 overflow-hidden rounded-xl bg-white ring-1 ring-slate-200">
            <Image src="/logo.png" alt="Caviar Larimi Logo" fill className="object-cover" />
          </div> <div className="leading-tight"> <p className="text-sm font-extrabold tracking-tight text-slate-900">Caviar Larimi</p> <p className="text-[11px] font-semibold text-slate-500">{t('tagline')}</p> </div> </Link> <nav className="hidden items-center gap-6 md:flex"> <Link className="text-sm font-semibold text-slate-700 hover:text-brand-800" href="/#products">{t('products')}</Link> <Link className="text-sm font-semibold text-slate-700 hover:text-brand-800" href="/offer">{t('offers')}</Link> <Link className="text-sm font-semibold text-slate-700 hover:text-brand-800" href="/contact">{t('contact')}</Link> </nav> <div className="flex items-center gap-2"> {locales.map((l) => ( <Link key={l} href={`/${l}${pathnameWithoutLocale}`} className={`rounded-lg px-3 py-2 text-xs font-bold transition ${ l === locale ? 'bg-brand-700 text-white' : 'bg-white text-slate-700 hover:bg-brand-50' }`} aria-label={`Switch language to ${localeNames[l]}`} > {localeNames[l]} </Link> ))} <Link href="/offer" className="ml-2 hidden luxe-button md:inline-flex">{t('cta')}</Link> </div> </div> </header> );
}
